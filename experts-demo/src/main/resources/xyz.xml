<project name="xyz-analysis" default="test" basedir="."> 

    <target name="xyz-analysis" extensionOf="xyz">
        <java classname="p4analyser.experts.demo.App"> 
            <arg value="${host}"/>
            <arg value="${port}"/>
            <arg value="${remoteTraversalSourceName}"/>
        </java>
    </target>

</project>
