<project name="demo-analysis" default="test" basedir="."> 

    <target name="demo-analysis" extensionOf="demo">
        <java classname="p4analyser.experts.demo.App"> 
            <arg value="${host}"/>
            <arg value="${port}"/>
            <arg value="${remoteTraversalSourceName}"/>
        </java>
    </target>

</project>
