<project name="call-graph-expert" default="test" basedir="."> 
    <target name="call-graph-implem" extensionOf="call-graph">
        <java classname="p4analyser.experts.callgraph.CallGraphGenerator">
            <arg value="${host}"/>
            <arg value="${port}"/>
            <arg value="${remoteTraversalSourceName}"/>
        </java>
        
        <echo message="OK"></echo>
    </target>

</project>
