
<project name="control-flow-expert" default="test" basedir="."> 
    <target name="control-flow-implem" extensionOf="control-flow">
        <java classname="p4analyser.experts.controlflow.ControlFlow">
            <arg value="${host}"/>
            <arg value="${port}"/>
            <arg value="${remoteTraversalSourceName}"/>
        </java>
        
        <echo message="OK"></echo>
    </target>

</project>
